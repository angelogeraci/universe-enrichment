describe('Accès à la page admin selon le rôle', () => {
  it('refuse l\'accès à /admin pour un utilisateur non admin (à activer quand la gestion des rôles sera en place)', () => {
    // Connexion avec un utilisateur non admin (à adapter quand tu auras un user non admin)
    // cy.visit('/login')
    // cy.get('input[name="email"]').type('user@demo.com')
    // cy.get('input[name="password"]').type('userpassword')
    // cy.get('button[type="submit"]').click()
    // cy.visit('/admin', { failOnStatusCode: false })
    // cy.url().should('not.include', '/admin')
    // cy.url().should('include', '/login')
  })
}) 